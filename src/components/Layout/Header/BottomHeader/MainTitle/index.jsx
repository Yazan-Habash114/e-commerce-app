import Typography from "@mui/material/Typography";

const MainTitle = () => {
  return (
    <Typography
      variant="h5"
      component="div"
      sx={{
        fontStyle: "normal",
        fontWeight: "700",
        fontSize: "24px",
        color: "black",
      }}
    >
      Exclusive
    </Typography>
  );
};

export default MainTitle;
